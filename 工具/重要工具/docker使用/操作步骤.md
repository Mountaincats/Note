```bash
docker pull mountaincats/ysyx:latest
# 验证拉取结果
docker images
# 能看到 mountaincats/ysyx:latest 镜像即为成功
# 启动容器
docker run -it --name ysyx-server mountaincats/ysyx:latest
```

```bash
# 按exit退出
# 若想删除容器
docker rm ysyx-server
# 若想删除镜像
docker images # 找镜像ID
docker rmi <ID>
```